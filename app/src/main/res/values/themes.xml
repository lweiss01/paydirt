<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme — Compose takes over immediately after launch.
         This just prevents a white flash on startup. -->
    <style name="Theme.PayDirt" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0A0F1E</item>
        <item name="android:statusBarColor">#0A0F1E</item>
        <item name="android:navigationBarColor">#0A0F1E</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
